import 'package:flutter/material.dart';

class HotelGridView extends StatelessWidget {
  const HotelGridView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
